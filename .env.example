# Environment example for Token Arena
# Copy to .env and adjust values as needed

DATABASE_URL=sqlite:///token_battles.db
SECRET_KEY=change-me
DEBUG=1

# Optional: Flask-Limiter storage
# For dev: memory:// (default). For prod: use Redis or Memcached, e.g.
# LIMITER_STORAGE_URI=redis://localhost:6379

# Optional: Session lifetime (seconds); default 30 days
# SESSION_LIFETIME_SECONDS=2592000

# Optional: Avatar upload size limit (bytes); default 2MB
# AVATAR_MAX_BYTES=2097152

# Optional: S3 avatar uploads (enable presigned upload flow)
# S3_AVATAR_BUCKET=your-bucket
# S3_REGION=us-east-1
# S3_ACCESS_KEY_ID=AKIA...
# S3_SECRET_ACCESS_KEY=...
# S3_ENDPOINT_URL=https://<custom-endpoint>   # optional (MinIO / R2)
# S3_PUBLIC_BASE_URL=https://cdn.example.com   # optional public base URL override

# --- SEO / Site ---
# SITE_NAME=Token Arena
# SITE_URL=https://tokenarena.example
# DEFAULT_DESCRIPTION=Battle-ready market intel: leaderboards, holders, charts and competitions.
# DEFAULT_SOCIAL_IMAGE=/static/og-default.png
# STATIC_VERSION=1
# STATIC_CACHE_SECONDS=86400

# --- Analytics ---
# ANALYTICS_PLAUSIBLE_DOMAIN=tokenarena.example
# ANALYTICS_GA4_ID=G-XXXXXXXXXX
# TWITTER_SITE=@tokenarena

# --- Web server ---
# PORT=8080
# WORKERS=4

# --- Redis / RQ Worker ---
# REDIS_URL=redis://localhost:6379/0
# RQ_QUEUES=default
# NOSTR_SCHEDULE_SECONDS=60
# FUNDS_SCHEDULE_SECONDS=60

# --- RLN (RGB Lightning Node) ---
# RLN_BASE_URL=http://localhost:3001
# RLN_BEARER= # optional bearer token
# RLN_BASIC_USER= # optional basic auth user
# RLN_BASIC_PASS= # optional basic auth pass
# RLN_TIMEOUT_SECONDS=20

# --- Platform account for fees (user id in DB) ---
# PLATFORM_USER_ID=1

# --- Admin UI access (one of these must match your session user) ---
# ADMIN_USER_ID=1              # ID of the admin user (from users.id)
# ADMIN_NPUB=ffffffff...       # Optional: admin user's Nostr hex pubkey

# Nostr polling task defaults
# NOSTR_RELAY_URL=wss://relay.damus.io
# NOSTR_FILTERS={"kinds":[1],"limit":20}
# NOSTR_POLL_SECONDS=8
# NOSTR_MAX_EVENTS=25
# NOSTR_SCHEDULE_SECONDS=60
